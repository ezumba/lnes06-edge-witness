<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- ExergyNet Hardware Kinesis Permissions -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.HIGH_SAMPLING_RATE_SENSORS" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <!-- LNES WiFi LAN rail: NSD/mDNS service discovery + multicast lock so two
         phones on the same Wi-Fi find each other and exchange messages/calls over
         direct TCP. Needs no dangerous runtime permission (unlike BLE). -->
    <uses-permission android:name="android.permission.CHANGE_WIFI_MULTICAST_STATE" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <!-- Ring with the native call UI over the lock screen (incoming calls). -->
    <uses-permission android:name="android.permission.USE_FULL_SCREEN_INTENT" />
    <!-- BLUE TEAM (H4): READ_CONTACTS + CALL_PHONE removed alongside the pruned
         getPhoneContacts / makePhoneCall bridge methods. ExergyNet is a compute and
         sensor verification network, not a telephony dialer. -->
    <uses-feature android:name="android.hardware.camera" android:required="true" />

    <!-- Ambient / Audio capture -->
    <uses-permission android:name="android.permission.RECORD_AUDIO" />

    <!-- NFC/RFID physical tap -->
    <uses-permission android:name="android.permission.NFC" />
    <uses-feature android:name="android.hardware.nfc" android:required="false" />

    <!-- Network density scan (Bluetooth LE beacon density) -->
    <uses-permission android:name="android.permission.BLUETOOTH" />
    <uses-permission android:name="android.permission.BLUETOOTH_SCAN" android:usesPermissionFlags="neverForLocation" />
    <uses-permission android:name="android.permission.BLUETOOTH_CONNECT" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADVERTISE" />
    <uses-permission android:name="android.permission.CHANGE_WIFI_STATE" />
    <uses-permission android:name="android.permission.NEARBY_WIFI_DEVICES"
        android:usesPermissionFlags="neverForLocation" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_CONNECTED_DEVICE" />
    <!-- Android 14+: required to run the call FGS with the microphone type so the
         OS does not asphyxiate the active voice pipeline in the background. -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MICROPHONE" />
    <!-- Android 14+: dataSync is a normal (auto-granted) FGS type. We boot the
         relay service as dataSync so it NEVER crashes at startup before the user
         has granted a Bluetooth permission (connectedDevice would throw and kill
         the service). Once BLE perms land, the banner is promoted to
         connectedDevice. -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" />

    <!-- Voice call audio routing -->
    <uses-permission android:name="android.permission.MODIFY_AUDIO_SETTINGS" />

    <!-- In-app APK self-update: allow install from our own FileProvider URI -->
    <uses-permission android:name="android.permission.REQUEST_INSTALL_PACKAGES" />

    <!-- AREM: cellular tower reading -->
    <uses-permission android:name="android.permission.READ_PHONE_STATE" />

    <!-- Passive background sensor access -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />

    <!-- THE APPLICATION MEMBRANE -->
    <application
        android:allowBackup="false"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="LNES-06 Edge Witness"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MyApplication"
        android:networkSecurityConfig="@xml/network_security_config"
        tools:targetApi="31">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:configChanges="orientation|screenSize|screenLayout|keyboardHidden">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- ExergyNet Disk Kinesis Provider (STRICTLY INSIDE APPLICATION TAG) -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

        <!-- Native A/V call UI (hosts WebRTC SurfaceViewRenderers — the WebView
             cannot render WebRTC video). Launched by DLTNForegroundService on
             RINGING/CALLING; rings over the lock screen. -->
        <activity
            android:name=".CallActivity"
            android:exported="false"
            android:launchMode="singleTop"
            android:showWhenLocked="true"
            android:turnScreenOn="true"
            android:configChanges="orientation|screenSize|screenLayout|keyboardHidden" />

        <!-- DLTN Mesh Relay Foreground Service -->
        <service
            android:name=".DLTNForegroundService"
            android:exported="false"
            android:foregroundServiceType="connectedDevice|microphone|dataSync" />

    </application>
</manifest>